1) Initial commit
Added Instruction for markdown, C# Expert, Angular from github-awesome repo
Added GitHub MCP Server
My first Prompt - 
Create a new branch then checkout them push commit with all files and create pull request
Use MCP --- Done without any issues
First problem - no ssh key to pull new changes, I asked free model to tell me how to add ssh key -- Fixed 
2) Creating a plan of my project
I am starting a ai course, so i need to implement a project with ai only, read requirements below and analyze them. 
PROMPT:
Goal
Deliver a working application where at least 90% of the code is generated by AI tools, and you act mainly as reviewer.
The idea is up to you (e.g. a small UI + API app), but it should include at least:
Several pages and components
Routing
A few different layouts (e.g. main layout, admin layout, etc.)
What you must deliver
Working project
A runnable solution (UI + optional API).
Code is mostly generated with Copilot with your review and minimal manual editing.
Prompt & workflow log (documentation)
A short document describing how you used AI to build the app:
All key prompts you used, in order (initial, refined, fixes, refactors, tests, etc.).
The context you provided (project description, constraints, existing code).
The models/tools you used (e.g. GitHub Copilot in VS / VS Code, ChatGPT, etc.).
Any used MCP servers or external tools (what for and how).
For each important step, show:
The prompt you used
The result (summary, not full code)
What you accepted/changed and why
Insights
Short section with your observations and learnings:
Which prompts worked well?
Which prompts did not work, and why?
What patterns in prompting gave the best results (e.g. “first ask for architecture, then ask for implementation per file”, etc.)?
Format
Project: in a repository (solution should build & run).
Documentation: README.md (or similar) in the repo with:
Overview of the app
Prompt history (key steps)
Tools/models/MCP used
Insights and recommendations for future use of Copilot.
 
Project Ideas for the Course
Skill Extraction Tool
An app that reads a new employee’s CV and IFU, extracts skills, and saves them in a structured format.
Test Checking Assistant
A system that checks paper tests, gives feedback based on mistakes, and creates a list of reading materials for improvement.
Legal Document Comparator
A tool for lawyers: it reads new versions of legal documents, compares them with old ones, and sends a summary with key changes and their meaning.
Performance Review Reminder Bot
A bot that checks who has performance reviews this month, sends reminders to team members, and reports missing feedback to the team manager.
Codebase Technology Scanner
A bot that scans a project folder, finds all tools/frameworks with versions, and shows what technologies are active or not. LLMs help extract this information.

After that we need to create a plan of my application. I want to create an application for reading pdf files, i want to upload them in or system, translate them, and create a pdf with translated text in the same format as original and download them. Possible languages are Russian, English, Polish, but might be others in the future. Document format is pdf, but can be word in the future. Use Angular 20, .net csharp and sqllite db. We will add docker in the future.
You main task right now is to suggest me a a project structure, technologies, ways of implementation  
---- how you transform translated text into a pdf?
---- lets save translated text from the pdf somewhere without creating a new pdf for the first phase and i want to add possiblity to see that text on ui somehow
-- good, show me the full project structure
--on UI we should add possibility to set translate language by default it is russian
on My translations page add a link to translated file
--What if I want to change the language of translation,  how you save it and show in the app?
---if the file is translated on 2 or more languages I want to see that, have possibility to see that and don't translate it again
![alt text](image.png)
--- cool, i like it
tell me about admin page, why do i need it, what can be done via that page

--- let's create an application plan, create an instruction with structure description, then create folder docs with a feature descriptions
--- problems
![alt text](problem.png)
Result of planning:
![alt text](image-1.png)

3) Split features into tasks
Create a new prompt to split features
    I want to create split-feature prompt
    I want this prompt to do the next things
    1. Add new folder with name "file_name-tasks"
    2. Read the file and create tasks from user stories
    3. Split UI and api tasks into small pieces
    4. add tag UI, API, DB into task name
Create a new prompt for pull request
    create prompt pull-request
    this prompt should create a new branch
    checkout this branch
    commit all files with a small description
    push to the server
    and create a pull request to main branch
    Use MCP
    ---problem stuck after pushing --- 
    ![alt text](image-2.png)
    How fixed - manually
4) I used my implement-task prompt to implement tasks
   and commit them one by one in the feature/auth-tasks for checking
   Example of auth-task 4 result 
   ![alt text](image-3.png)
   -- I update my prompt if I see any issues or modifications f.e
      Delete all unnecessary imports in the code files after implementation to keep the code clean and maintainable.
   -- My first swagger run
   ![alt text](image-4.png)   
   -- While implemented task 09(first fronted task) I wanted to init ui app, asked copilot, but copilot created a lot of mocked components from the future tasks, so I had to undo the whole task
5) First App init with a prompt I want to init api and ui projects and see them in a browser
   -- What I saw
   ![alt text](image-5.png)
   -- After fix: delete standard angular html, show only my application layouts
   ![alt text](image-6.png)
   ![alt text](image-7.png)
   I registed and tried to log in with incorrect password
  ![alt text](image-9.png)
  6) As a result of first implementation
  - I created a new agent for ui 
  - I added a new paraghraph in angular instruction - Use facade pattern for each componement to manage state and interactions with services, so no direct service calls in the component class or template
   - Review prompt: I want to you to check all frontend files and add a patern facade to all the components to avoid direct calls of data sources, try to add a facade to all the components and move all dependencies to this facade
   -update all task files with a new structure where needed
7) Created tasks for the next feature
   copilot didn't mark file as done and didn't add notes -fix by prompt: mark that task as done do it the same way as for auth-feature
   Problem solving: ![alt text](image-10.png)
   -- While i implemented task 3 copilot found a dependency on Task 4 and started to do both at the same time (i missed that dependency), so i had no objections
   ![alt text](image-11.png)
   --problem: the same issue for task 07 copilot didn't mark file as done and didn't add notes -fix by prompt: mark that task as done do it the same way as for auth-feature
   --problem: copilot don't use facade pattern for ui
   --fix: updates agent and intruction
   **Critical rule — Facade Pattern**: Every component MUST have a co-located facade (`<component-name>.facade.ts`) declared in the component's `providers` array. Components may only inject their own facade — never stores, services, or repositories directly. The facade handles all state and service orchestration.
8) Second feature initial run
   problem - no languages in the list
![alt text](image-12.png)
   analizing - ![alt text](image-13.png) - result: this control will be done in admin feature
9) Admin Page feature
   I changed implement-task prompt to do all the tasks from the context: our task is to implement all tasks from context files.
   When I split the feature copilot created 19 tasks,
   so I asked him to combine them all.
   --can you combine some tasks into a one?![alt text](image-14.png)
   BUGS:
   --i see an issue with Http failure response for http://localhost:4200/api/languages: 404 Not Found
   ![alt text](image-15.png)
   --I want to see all the links in a side bar if I am admin without back links, so copilot created a sidebar for me -- I missed that feature at all
   ![alt text](image-16.png)
10) New feature - i asked copilot to suggest me the next feature 
![alt text](image-17.png)
 review: ![alt text](image-18.png)
 deepl key --![alt text](image-19.png)
 bug: when i click to button translate it redirects me to login page and no requests for translating are sent to api; check guards
 BUG:![alt text](image-20.png)
 BUG: ✕ DeepL API returned 403: {"message":"Legacy authentication method 'form body' is no longer supported. Please update to header-based authentication. Find more details in our docs: https://developers.deepl.com/docs/resources/breaking-changes-change-notices/november-2025-deprecation-of-legacy-auth-methods. You can find more info in our docs: https://developers.deepl.com/docs/getting-started/auth"}
 bug: on supported language page i see a problem with english flag
 modification prompt: Let's add default source language and target language on upload page; add field isDefault for language, set english for source and russian for target and add preselection on UI
 Translate button should be disabled unit all validations passed

 Bug: At the moment, while logged in under the admin account, on the **My Translations** page under the file **English.pdf** I see **6 failed RU translations** and **2 successful RU translations**.

Why am I seeing so many of them? I expect to see **only one status per translation**.

bug:if file format is not correct then background of drop down area should be red
first issue: ![alt text](image-22.png)
![alt text](image-21.png)
11) Analyzing project to check if it includes all requirements
![alt text](image-23.png)
![alt text](image-24.png)
prompt: check my comments folder i added a lot of comments here; it will be useful for you to create readme file; use all the material here


Prompts examples:
I want to add unit tests for the ui app; suggest me a framework -- jest
I want to add tests for components, facades and pipes

Create a feature for unit testing from your plan
Create prompt to implement unit tests for ui 
1) add instruction to create mock and stubs for the services 
2) use angular best practices from angular.instruction
3) after implementatition check and verify the code
4) run unit tests and ask me to fix failed test