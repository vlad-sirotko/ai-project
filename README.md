## PDF Translation App

A full-stack web application that allows users to upload PDF files, translate them into
multiple languages using AI-powered translation (DeepL or Mock provider), and read the
translated text directly in the browser. Built as a course project with 90%+ of the code
generated by GitHub Copilot.

> **Course project** — developer acted as reviewer, prompt engineer, and decision-maker.
> Minimal manual code editing.

---

## Overview

| Layer | Technology |
|---|---|
| Frontend | Angular 20 (standalone components, signals, Angular Material) |
| Backend | ASP.NET Core 8, Clean Architecture (Domain / Application / Infrastructure / API) |
| Database | SQLite via EF Core 8 |
| Auth | JWT Bearer tokens, BCrypt password hashing |
| Translation | DeepL API or Mock provider (switchable at runtime via admin panel) |
| PDF Extraction | PdfPig (MIT license, no native dependencies) |

### Pages & Layouts

| Layout | Pages |
|---|---|
| `AuthLayout` | Login, Register |
| `MainLayout` | Upload, My Translations, Translation Detail |
| `AdminLayout` | Admin Settings, Admin Languages |

### Key Features

- Upload a PDF and queue a background translation job via `Channel<Guid>`
- Real-time status polling (3 s on detail page / 5 s on list page) using Angular signals + RxJS intervals
- One document can be translated into many languages simultaneously — results stored and displayed per language tab
- SHA-256 duplicate detection — re-uploading the same file reuses the existing document record
- Pluggable translation provider: swap Mock ↔ DeepL without restart or code change
- Admin panel: manage supported languages and translation provider/API key settings

---

## How to Run

### Backend

```bash
cd backend
dotnet restore
dotnet run --project TranslationApp.API
```

API runs at `http://localhost:5000`. Swagger UI at `/swagger`.
SQLite database (`app.db`) is created and migrated automatically on first run.
Default seed admin credentials are in `TranslationApp.API/appsettings.Development.json`.

### Frontend

```bash
cd frontend
npm install
ng serve
```

App runs at `http://localhost:4200`. API requests proxied via `proxy.conf.json`.

### Admin Access

A default admin account is seeded automatically on first run. Use these credentials to log in:

| Field | Value |
|---|---|
| Email | `admin@translationapp.dev` |
| Password | `Admin123!` |

After logging in, the **Admin** layout becomes available with two pages:

- `/admin/settings` — manage the active translation provider and API key
- `/admin/languages` — add languages or toggle them active/inactive

### Switching to DeepL

By default the app uses the **Mock** provider (returns placeholder text instantly, no API
key required). To enable real translation:

1. Log in as admin and navigate to **Admin → Settings**.
2. Change the **Translation Provider** dropdown from `Mock` to `DeepL`.
3. Enter a valid DeepL API key in the **API Key** field. A free-tier key is already
   available for local development:
   ```
   30d05b64-85b3-47d8-926e-7e1da9155a26:fx
   ```
4. Check the **Use Free DeepL API** checkbox — free-tier keys end in `:fx` and require
   this flag so the provider targets the correct API endpoint.
5. Click **Save** — the change takes effect immediately for all subsequent jobs without
   restarting the server.

A working DeepL Free-tier API key is already pre-configured in
`TranslationApp.API/appsettings.Development.json` for local development.

> **Note:** The DeepL Free tier allows up to **500,000 characters/month**. If translation
> jobs start returning 403 or quota errors, the monthly limit has been reached — switch
> back to the **Mock** provider until the quota resets.

---

## Tools & Models Used

| Tool / Model | Purpose |
|---|---|
| **GitHub Copilot in VS Code** | Primary code generation — all features, fixes, refactors |
| **Claude Sonnet 4.6** (via Copilot) | Planning, architecture discussions, complex multi-file implementations |
| **GitHub MCP Server** | Branch creation, commits, pull requests via natural language prompts |
| **`CSharpExpert` agent** | Backend tasks — C#, ASP.NET Core Clean Architecture, EF Core |
| **`angularExpert` agent** | Frontend tasks — Angular 20, signals, facade pattern, Material |

### Custom Prompt Files

| Prompt | Location | Purpose |
|---|---|---|
| `implement-task` | `.github/prompts/implement-task.prompt.md` | Implements all tasks from context files end-to-end |
| `split-feature` | `.github/prompts/split-feature.prompt.md` | Splits a feature doc into granular [UI]/[API]/[DB] task files |
| `pull-request` | `.github/prompts/pull-request.prompt.md` | Creates branch, commits, pushes, and opens PR via MCP |

### Configuration & Context Artifacts

- `.github/instructions/` — Project structure rules, Angular standards, C# standards, Markdown rules
- `.github/agents/` — `CSharpExpert.agent.md`, `angularExpert.agent.md`
- `docs/features/` — Six feature documentation files + ~58 granular task files

---

## Insights

### What worked well

**Architecture-first prompting** — Defining the full project structure, constraints, and
conventions in instruction files before writing any code gave Copilot consistent, persistent
context across every session. The quality of generated code was noticeably higher when the
architectural guardrails were already in place.

**Task decomposition workflow** — The pipeline of `feature doc → split-feature prompt →
task files → implement-task prompt` was the single most effective pattern in the project.
Each task was small enough for Copilot to implement correctly in one pass, with minimal
back-and-forth.

**Iterative prompt refinement** — Treating prompt files as living documents (adding "delete
unused imports" mid-implementation, escalating the facade rule on repeated violations)
improved output quality incrementally. Each refinement carried forward to all future tasks
automatically.

**Specialized agents** — Giving `CSharpExpert` and `angularExpert` layer-specific context
(Clean Architecture rules for C#, facade + signals rules for Angular) produced more
architecturally consistent output than a generic agent would.

**Error-driven bug fixing** — Pasting the exact error message and stating the expected
behaviour ("I see X, I expect Y") was consistently effective. Most bugs were fixed in a
single attempt.

**Asking Copilot to suggest next steps** — When unsure what to tackle next, asking Copilot
for a recommendation produced well-reasoned, prioritised suggestions. Used successfully
before the DeepL integration step.

---

### What did not work

**Broad initialization prompts** — "Init the UI app" was too vague. Copilot generated
mocked implementations for all future features preemptively, requiring a full undo.
Lesson: scope every initialization prompt explicitly ("scaffold routing only",
"add layouts only", not "set up the whole frontend").

**Copilot skipping task completion markers** — Without an explicit rule, Copilot would
implement the code correctly but forget to update the task file status and add implementation
notes. Required a corrective prompt every time until the rule was added permanently to
`implement-task.prompt.md`.

**Facade pattern drift** — Even after adding the facade rule to the instruction file, some
generated components still skipped it. Required escalating the rule to "Critical" in both
the agent definition and the instructions file before it was consistently followed.

**No prompt for branch, push, and PR automation** — The git workflow (create branch,
commit, push, open PR) was never packaged into a reusable prompt file. Every release cycle
required manually kicking off the sequence, which broke the otherwise fully prompt-driven
flow. A dedicated `pull-request.prompt.md` should have been one of the very first prompts
created, not an afterthought.

**Context-switching between agents and modes** — On several occasions work was started in
the wrong agent — firing a C# task at `angularExpert`, or an Angular task at `CSharpExpert`
— only to notice mid-implementation that the output felt off. Similarly, forgetting to
switch from Agent mode to Ask or Plan when doing exploratory work led to unnecessary file
edits and wasted context. A quick mental checklist before each task (right agent? right
mode?) would have prevented these interruptions entirely.

---

### Best Prompting Patterns

1. **Architecture before implementation** — Spend time upfront defining structure, naming
   conventions, and layer responsibilities in instruction files. Copilot generates much
   better code when the guardrails already exist.

2. **One task = one unit of work** — The smallest independent, tagged task ([UI]/[API]/[DB])
   consistently had the highest first-attempt success rate. Mixing layers or scoping too
   broadly increased failure rate and introduced inconsistencies.

3. **State the observable fact, not the fix** — "I see error X / I expect Y" outperforms
   "fix the bug". Copilot needs observable evidence to reason from, not guesses.

4. **Update prompts immediately on failure** — When Copilot misses a rule, add that rule to
   the prompt or instruction file before the next task. Repeating corrections conversationally
   does not propagate across sessions.

5. **Ask for suggestions when stuck** — Asking Copilot "suggest the next feature to
   implement" when uncertain about priorities produces actionable, context-aware
   recommendations.

6. **Consolidate before implementing** — When a split-feature prompt produces too many
   tasks (e.g., 19 admin tasks), ask Copilot to combine related ones before starting
   implementation. Fewer, slightly broader tasks reduce context-switching overhead.

---

## Prompt History — Key Steps

### Step 1 — Repository bootstrap & MCP setup

**Prompt:**
> Create a new branch, checkout, commit all files with a small description, push to server,
> and create a pull request to main. Use MCP.

**Tools used:** GitHub MCP Server

**Result:** Branch created, files committed, and PR opened to main — all via the MCP tool,
no manual git commands needed.

**Accepted/changed:** Everything accepted as-is.

**First issue encountered:** No SSH key configured on the machine, so pulling the new branch
failed. Asked a free AI model for SSH setup instructions — resolved in a few minutes.

---

### Step 2 — Project planning & architecture

**Context provided:** Course requirements (working app + prompt log), personal project idea.

**Prompt:**
> I want to create an application for reading PDF files — upload them, translate them, and
> download a translated PDF in the same format. Possible languages: Russian, English, Polish.
> Use Angular 20, .NET C#, SQLite. Suggest a project structure, technologies, and ways of
> implementation.

**Iterative follow-up prompts (in order):**

- *"How do you transform translated text into a PDF?"*
  → Decided to skip PDF generation in Phase 1; save translated text to DB and show it in
  the browser instead.
- *"Show me the full project structure."*
- *"Add possibility to set translate language on UI; default is Russian."*
- *"If the file is translated into 2+ languages, I want to see that and not translate it
  again."*
- *"Tell me about the admin page — why do I need it, what can be done via it?"*
- *"Create an instruction with structure description, then create a folder `docs` with
  feature descriptions."*

**Result:** Full Clean Architecture project layout defined. Six feature documentation files
created under `docs/features/`. Project structure instruction file added to
`.github/instructions/project-structure.instructions.md`.

**Accepted/changed:** All proposals accepted. PDF generation deferred to a future phase to
keep scope manageable. Admin panel concept fully adopted after Copilot explained its value.

![Project structure after planning](.github/my-comments/image-1.png)

---

### Step 3 — Custom prompt engineering

**split-feature prompt:**

> I want to create a split-feature prompt that:
> 1. Adds a new folder named `{file_name}-tasks/`
> 2. Reads the file and creates tasks from user stories
> 3. Splits UI and API tasks into small independent pieces
> 4. Adds tags [UI], [API], [DB] to each task name

**Result:** `.github/prompts/split-feature.prompt.md` created. Applied to all six feature
files — produced ~58 granular task files covering the entire backlog.

**pull-request prompt:**

> Create a new branch, checkout, commit all files with a small description, push to server,
> and create a pull request to main. Use MCP.

**Result:** `.github/prompts/pull-request.prompt.md` created.

**Problem:** Copilot completed branch creation and commit, but stalled after push without
opening the PR. **Fixed manually** for that iteration.

![Pull-request prompt stalling after push](.github/my-comments/image-2.png)

**implement-task prompt:**

> Implement all tasks from context files. Check code after implementation. Once verified,
> update the task file with implementation notes and `## Status: ✅ Done`.

**Result:** `.github/prompts/implement-task.prompt.md` — the primary implementation driver
for the entire project.

---

### Step 4 — Auth feature implementation

**Prompt used:** `implement-task` with all 16 `auth-tasks/` files in context.

**Process:** Tasks implemented and committed one by one to the `feature/auth-tasks` branch
for incremental review. Order: DB entities → API handlers → JWT service → Angular
components, store, guards, interceptor.

![Auth task 04 — JWT service implementation result](.github/my-comments/image-3.png)

**Mid-implementation refinement added to prompt:**
> "Delete all unnecessary imports in the code files after implementation to keep the code
> clean and maintainable."

**Result:** Working auth flow — register, login, JWT generation, route guards. First Swagger
run confirmed the API was functional.

![First Swagger run — auth endpoints confirmed](.github/my-comments/image-4.png)

**Problem:** On Task 09 (first Angular task), the prompt "init the UI app" was too broad.
Copilot generated mocked implementations of all future features preemptively — had to undo
the entire task and restart with a more scoped prompt.

---

### Step 5 — First full app run

**Prompt:**
> I want to init the API and UI projects and see them in a browser.

**Result:** Both projects running simultaneously. Angular showed the default CLI placeholder.

![Angular default placeholder on first run](.github/my-comments/image-5.png)

**Fix prompt:**
> Delete the standard Angular HTML; show only my application layouts.

**Outcome:** Auth layout, main layout, and admin layout all rendering. Register and login
tested — incorrect password shows error correctly.

![Sign in page after fix](.github/my-comments/image-6.png)
![Register page after fix](.github/my-comments/image-7.png)
![Invalid password error handling](.github/my-comments/image-9.png)

---

### Step 6 — Facade pattern enforcement

**Problem discovered:** After the first full run, components were injecting stores and
services directly — violating separation of concerns and making future testing harder.

**Review prompt:**
> Check all frontend files and add the facade pattern to all components. Move all
> dependencies (stores, services) to a co-located facade. No direct store or service calls
> from the component class or template.

**Result:** All components refactored. `angularExpert` custom agent created.
Angular instruction file updated with a critical rule:

> **Critical rule — Facade Pattern:** Every component MUST have a co-located
> `<component-name>.facade.ts` declared in the component's `providers` array. Components
> may only inject their own facade — never stores, services, or repositories directly.

All existing task files updated to reflect the new structure requirement.

---

### Step 7 — Document upload feature

**Prompt used:** `implement-task` with `document-upload-tasks/` context.

**Notable:** On Task 3 (upload handler), Copilot identified a dependency on Task 4
(SHA-256 duplicate detection) and implemented both in the same pass. The developer had missed
that dependency in the task list — no objections raised.

![Copilot detecting task dependency between upload handler and SHA-256 tasks](.github/my-comments/image-11.png)

**Recurring problem:** Copilot not marking task files as done after implementation.
**Fix prompt used each time:**
> "Mark that task as done, do it the same way as for the auth feature."
→ The completion requirement was later added permanently to `implement-task.prompt.md`.

![Marking document-upload task as done](.github/my-comments/image-10.png)

**Recurring problem:** Some UI components still bypassing the facade rule.
**Fix:** Upgraded the rule to "Critical" in both `angularExpert.agent.md` and
`angular.instructions.md`.

---

### Step 8 — Admin panel feature

**Prompt change:** Updated `implement-task` to process all tasks at once:
> "Our task is to implement all tasks from context files."

**Task count:** `split-feature` initially produced 19 admin tasks.
> "Can you combine some tasks into one?"
![Prompting Copilot to add admin sidebar navigation links](.github/my-comments/image-14.png)
→ Consolidated to 8 tasks without losing any functionality.

**Bugs fixed via prompts:**

- `GET /api/languages` returning 404 → proxy configuration issue; pointed Copilot at the
  error and it fixed `proxy.conf.json`.
 ![GET /api/languages returning 404 Not Found](.github/my-comments/image-12.png)
![GET /api/languages returning 404 Not Found](.github/my-comments/image-13.png)


- Admin sidebar missing navigation links → prompted Copilot to add a full sidebar with
  Settings and Languages links (a feature that had been missed entirely in the original spec).
![Admin sidebar showing all links for admin users](.github/my-comments/image-15.png)
![Copilot-suggested translation flow implementation plan](.github/my-comments/image-16.png)

---

### Step 9 — Translation flow + DeepL integration

**Prompt:**
> Suggest the next feature to implement.

**Result:** Copilot recommended completing the background translation pipeline and adding
real DeepL integration as the highest-value next step — agreed and proceeded.
![PdfTextExtractor and DeepL provider implementation in VS Code](.github/my-comments/image-17.png)

**DeepL API key configuration:**
> "I got an api key for deepl, save it in appsettings and use as a default key."

→ Copilot updated `appsettings.Development.json` with the key as the default DeepL setting.

![Providing DeepL API key via prompt](.github/my-comments/image-18.png)

**Bugs encountered and fixed via prompts:**

- *Admin settings 400 error — `Settings` field required:* Prompt: *"I want to change
  translation provider to DeepL but see 400."* → Copilot fixed the request payload in
  the admin settings form.

![Admin settings 400 validation error when switching to DeepL](.github/my-comments/image-20.png)

- *Translate button redirected to login page* — route guard incorrectly blocking the
  translate endpoint. Prompt: *"When I click Translate it redirects me to login; check
  guards."* → Fixed.

- *DeepL 403 error — legacy auth deprecated:*
  > `DeepL API returned 403: Legacy authentication method 'form body' is no longer supported.
  > Please update to header-based authentication.`
  → Copilot updated `DeepLTranslationProvider` to pass the API key via `Authorization`
  header instead of a form field.

- *English flag displaying incorrectly on admin languages page* → Prompt described the
  visual issue; Copilot fixed the flag emoji mapping.

- *6 failed + 2 successful RU jobs showing under one document* → Race condition in job
  deduplication logic; Copilot identified and fixed the duplicate-job creation path.

- *Upload dropzone not turning red on invalid file format* → Described expected visual
  behavior; Copilot updated the dropzone component styles.

**Feature additions via prompts:**
> "Add default source and target language on upload page. Add an `isDefault` field to
> languages — set English as default source, Russian as default target — and pre-select
> them on the UI."

> "Translate button should be disabled until all validations pass."

![Upload page — translate button disabled until all validations pass](.github/my-comments/image-19.png)

---

### Step 10 — Translations list feature

**Prompt used:** `implement-task` with `translations-list-tasks/` context.

**Tasks implemented:** `StatusBadgeComponent` (verified already fully implemented), `addLanguage` and `retry` methods added to `TranslationsListFacade`.

**Bug encountered:**

- *`EMPTY` not exported from `rxjs/operators`:* Copilot imported `EMPTY` from `rxjs/operators` instead of `rxjs`. Spotted in the chat and fixed by correcting the import path.
![Course requirements analysis prompt](.github/my-comments/image-22.png)
![Translations list tasks complete — EMPTY import issue flagged in chat](.github/my-comments/image-21.png)

---

### Step 11 — Final review & README creation

**Prompt:**
> "We finished our app. Let's analyze all the requirements and check what I forgot to do,
> and create me a list of your suggestions."

**Result:** Copilot reviewed course requirements against the current project state and
identified the biggest gap — the root `README.md` was still Angular CLI boilerplate.



![Copilot listing missing README sections required by the course](.github/my-comments/image-23.png)

**Action:** Prompted Copilot to generate the full `README.md` covering app overview,
prompt history, tools/models, and insights — producing the document you are reading now.
